# Survey Question Answers Fixtures
#
# This file is intentionally left empty as survey_question_answers are created dynamically
# in tests rather than through fixtures to avoid complex foreign key dependencies.
