<script>
  const currentUser = JSON.parse('<%= raw(current_user&.to_json(only: [:id, :login_name, :name], methods: [:avatar_url, :roles, :primary_role])) || "null" %>');
  const currentUserID = <%= current_user&.id || 0 %>;
  const signedIn = <%= current_user.present? ? 'true' : 'false' %>;
</script>
