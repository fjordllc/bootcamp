<script>
  var currentUser = <%= raw (current_user ? current_user : nil).to_json(only: [:id, :login_name, :name], methods: [:avatar_url, :daimyo?, :roles, :primary_role]) %>;
<% if logged_in? %>
  currentUser['daimyo'] = currentUser['daimyo?']
<% end %>
  var currentUserID = <%= raw current_user ? current_user.id : 0 %>;
  var signedIn = <%= current_user ? true : false %>;
</script>
