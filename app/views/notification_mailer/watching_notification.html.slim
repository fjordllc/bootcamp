= render 'notification_mailer_template', title: "#{@watchable.notification_title}に#{@comment.user.login_name}さんがコメントしました。", link_url: notification_url(@notification, anchor: "comment_#{@comment.id}"), link_text: "この#{@watchable.class.model_name.human}へ" do
  = md2html @comment.description
