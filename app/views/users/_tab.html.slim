h2= t('active_users', count: user_by_type(users.student.active, target).count)
= render 'user', users: user_by_type(users.student.active, target)
h2= t('inactive_users', count: user_by_type(users.student.inactive, target).count)
= render 'user', users: user_by_type(users.student.inactive, target)
h2 卒業生 (#{user_by_type(users.graduated, target).count})
= render 'user', users: user_by_type(users.graduated, target)
h2 アドバイザー
= render 'user', users: user_by_type(users.advisers, target)
