- year = buzzes_year(buzzes)
- month_buzzes = group_buzzes_by_month(buzzes)

h1 = year
- month_buzzes.each do |month, buzzes|
  h2 = "#{month}月"
  ul
    - buzzes.each do |buzz|
      li = link_to buzz.title, buzz.url, target: '_blank', rel: 'noopener noreferrer'
