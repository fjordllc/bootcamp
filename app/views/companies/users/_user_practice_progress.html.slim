.completed-practices-progress__bar-container
  .completed-practices-progress__bar
  .completed-practices-progress__percentage-bar(
    aria-valuemax='100'
    aria-valuemin='0'
    aria-valuenow='#{user.status.graduated? ? 100 : user_course_practice.cached_completed_percentage}'
    role='progressbar'
    style="width: #{user.status.graduated? ? '100%' : "#{user_course_practice.cached_completed_percentage.round}%"}")
.completed-practices-progress__number
  | #{user.status.graduated? ? '卒業' : user_course_practice.cached_completed_fraction}
