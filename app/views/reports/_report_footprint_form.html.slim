= form_for ([@report, @footprint]), html: {class: "report-footprint-form"} do |f|
  = hidden_field_tag :user_id, current_user.id
  = f.submit t('footprint_report'), class: 'report-footprint-form__action is-button-simple-sm-danger'
