li.footprints-item
  = link_to footprint.user, itempro: "url" do
    = image_tag footprint.user.avatar.variant(resize: "32x32"), class: "footprints-item__checker-icon is-#{footprint.user.login_name}", alt: footprint.user.login_name
