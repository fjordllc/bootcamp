li.footprints-item
  = link_to footprint.user, itempro: "url" do
    = gravatar_tag footprint.user, secure: true, size: 32, html: { class: "footprints-item__checker-icon is-#{footprint.user.login_name}", alt: footprint.user.login_name }
